<!DOCTYPE web-app PUBLIC
        "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
        "http://java.sun.com/dtd/web-app_2_3.dtd" >

<web-app>
    <display-name>Archetype Created Web Application</display-name>

    <!-- [Root WebApplicationContext] contextClass : ContextLoaderListener가 만들어 줄 애플리케이션 컨텍스트의 타입 지정 -->
    <context-param>
        <param-name>contextClass</param-name>
        <param-value>org.springframework.web.context.support.AnnotationConfigWebApplicationContext</param-value>
    </context-param>

    <!-- [Root WebApplicationContext] contextConfigLocation : 컨텍스트 설정파일(자바 or XML)의 위치 지정  -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>me.frenchline.AppConfig</param-value>
    </context-param>

    <!-- [Root WebApplicationContext] 스프링 IoC 컨테이너 : 스프링이 제공해주는 ContextLoaderListener -->
    <listener>
      <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <!-- [Servlet WebApplicationContext] 스프링 웹 MVC : 스프링이 제공하는 서블릿 구현체 DispatcherServlet -->
    <servlet>
        <servlet-name>app</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <!-- 이때 파라미터를 등록할 수 있다 -->
        <init-param>
            <param-name>contextClass</param-name>
            <param-value>org.springframework.web.context.support.AnnotationConfigWebApplicationContext</param-value>
        </init-param>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>me.frenchline.WebConfig</param-value>
        </init-param>
    </servlet>

    <servlet-mapping>
        <servlet-name>app</servlet-name>
        <url-pattern>/app/*</url-pattern>
    </servlet-mapping>

</web-app>
